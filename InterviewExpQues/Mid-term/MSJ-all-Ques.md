17/2/26

you have a feature branch in local, someone made the changes in remote-main, how do you update your feature branch

maven - how you do add dependency

maven - how do you remove the transitive dependency

What is JDK, JRE, JVM

code snippet : abstract Employee has private id and protected name, with abstract method calculateSalary(),   , 


there is a Developer class which extends Employee; Q1: how abstraction and encapsulation is achieved here , 

Q2: the calculateSalary() in Developer is doing method overloading or overriding

How many types of Inner classes are there, what are those, which one is used for lambdas

StringBuilder v/s StringBuffer, which is faster

Contract between equals() and hashCode()

Why do we use static keyword, give a real-time example

why do we use final class

finally v/s finalize()

who calls finalize()

Types of exception handling

throw v/s throws

nested try-catch, the exception is not handled in inner try's catch so what will happen, who would handle it

can the child throw ANY RuntimeException if the parent has declared a compile time exception/checked exception

What are generics, why we use 

What is WeakHashMap;  will the entry be deleted

Difference between ConcurrentHashMap and HashTable

if we add custom objects to TreeSet, how will be the sorting

we have an iterator and a for-loop, we keep adding an element to the list in for-loop, what will happen

how many abstract methods can a Functional Interface may have

what is flatMap, what does it return

what are records, how do you access its fields

what are sealed classes

what is Dependency Inversion principle

what is Liskov Substitution principle

difference between Decorator and Strategy

in a testing class, there are few variables that needs to initialized by the test methods and used by all test method : @BeforeEach

how do you read the class and message of the exception thrown , in the test class

there is a utility method call in test method, how would know, how many times it is called, altering the code

code snippet : Q: will the "Test" will printed??


 Optional<Integer> op = Optional.of(5);

 int a = op.orElse(test()); 



   public static int test(){ System.out.println("Test"); return 0; }

code questions : hint : using flatMap: 


Order{ List<Item> items; //getter methods}

Item{ String category; double price; //getters}

Q: store the sum of each category across all orders , example : "Category" : "its sum value"
